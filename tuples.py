#now a tupel is a typeof data structure which basically means it's a container where we can store different values. now if you're familiar with lists in python a tupel is actually very similar to a list.
coordinates = (4.555 , 5.005)
#you can NOT modify a tupel, once you create a tupel you cant change it
print(coordinates[1]) 
print(coordinates[0])
